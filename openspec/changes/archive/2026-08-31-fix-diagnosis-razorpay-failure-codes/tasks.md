## 1. Seed data: real Razorpay failure codes

- [x] 1.1 In `backend/recovery/management/commands/seed_data.py`, replace the invented codes in `PAYMENT_FAILURE_CODES` (`card_declined_expired`, `network_timeout`, `issuer_unavailable_timeout`) with real equivalents (`card_expired`, `payment_timed_out`, `bank_technical_error` or similar), keeping `insufficient_funds`, `card_declined`, and the blank/no-code entry as-is. Verify by reading the updated list and confirming no invented shorthand remains.
- [x] 1.2 Add a UPI-flavored weighted code pool (e.g. `UPI_FAILURE_CODES`, drawing from `invalid_vpa`, `vpa_resolution_failed`, `payment_collect_request_expired`, `bank_technical_error`, `payment_declined`, `payment_timed_out`, `payment_cancelled`) and fold it into `_seed_payment_degradation`'s code selection so a meaningful share of seeded payment-degradation transactions are UPI-flavored. Verify by running `python manage.py seed_data --flush` locally and confirming at least one seeded transaction has a UPI-specific failure code (e.g. via `Transaction.objects.filter(failure_code__in=[...])`).
- [x] 1.3 Replace `SUBSCRIPTION_FAILURE_CODES`'s invented codes (`mandate_charge_failed`, `card_declined_mandate`) with real mandate-context codes (e.g. `reqauth_mandate_not_acknowledged`, `mandate_creation_failed`, `funds_blocked_by_mandate`), keeping `insufficient_funds`, `card_declined`, and the blank entry. Verify by reading the updated list and confirming no invented shorthand remains.
- [x] 1.4 Run `python manage.py seed_data --flush` end-to-end locally and confirm it completes without error and produces the expected transaction counts (per existing `--payment`/`--subscription`/`--receivable` defaults).

## 2. Diagnosis heuristic: exact-match table + fallback tier

- [x] 2.1 In `backend/agents/pipeline.py`, add `_CODE_DIAGNOSES`, a dict keyed by exact Razorpay reason code, covering at minimum: `card_expired`, `insufficient_funds`, `card_declined`, `authentication_failed`, `card_not_enrolled`, `invalid_vpa`, `vpa_resolution_failed`, `bank_technical_error`, `gateway_technical_error`, `issuer_technical_error`, `transaction_limit_exceeded`, `incorrect_cvv`, `debit_instrument_blocked`, `debit_instrument_inactive`, `payment_timed_out`, `request_timed_out`, `payment_declined`, `payment_cancelled`, `payment_risk_check_failed`, plus the mandate-context codes added to seed data in 1.3 (`reqauth_mandate_not_acknowledged`, `mandate_creation_failed`, `funds_blocked_by_mandate`, and `recurring_payment_not_enabled`). Each entry carries `(root_cause, confidence, reasoning_text)` per design.md's Decision 1/2 guidance (inline comment on any non-obvious confidence choice, matching the existing code's convention). Verify by inspecting the dict for exact-string keys and no duplicates.
- [x] 2.2 Update `_heuristic_diagnosis` to check `_CODE_DIAGNOSES` (exact match) first, then fall back to the existing `_DIAGNOSIS_RULES` substring scan, then `_KIND_DEFAULTS`, per design.md Decision 1. Verify with a quick manual call (`_heuristic_diagnosis({"kind": "payment_degradation", "failure_code": "card_expired"})`) returning the table entry, not a substring-rule result.
- [x] 2.3 Add `timed_out` as an additional substring needle in `_DIAGNOSIS_RULES` (alongside the existing `timeout`) as defense-in-depth for codes outside the exact-match table, fixing the near-miss bug for any future/variant code shaped like `..._timed_out`. Verify `_heuristic_diagnosis` resolves a code like `some_future_timed_out_code` (not in `_CODE_DIAGNOSES`) to the timeout-class root cause via the substring tier.
- [x] 2.4 Update `_PAYMENT_DEGRADATION_ACTIONS` to map every new payment-degradation-applicable root cause from 2.1 to an explicit action, per design.md Decision 2's table (`authentication_failed`→`retry_order`, `card_not_enrolled`→`new_payment_link`, `invalid_vpa`→`new_payment_link`, `vpa_resolution_failed`→`retry_order`, the technical-error root cause→`retry_order`, `transaction_limit_exceeded`→`new_payment_link`, `incorrect_cvv`→`retry_order`, `debit_instrument_blocked`→`new_payment_link`, `debit_instrument_inactive`→`new_payment_link`; leave `risk_check_failed` unmapped so it falls through to the existing `.get(..., "escalate")` default — deliberately, since escalate is the correct outcome). Verify no new root cause from 2.1 that applies to `payment_degradation` is absent from the map without an explicit comment explaining why (only `risk_check_failed` should be the documented exception).
- [x] 2.5 Update `_SUBSCRIPTION_RETRIABLE_ROOT_CAUSES` to include the new root causes applicable to a subscription/mandate context, per design.md Decision 2 (all the new root causes except `risk_check_failed` and `recurring_payment_not_enabled`, both of which must escalate). Verify the set's membership against the design.md table.
- [x] 2.6 Update the module-level comments in `pipeline.py` that describe the rule table's structure/rationale (the existing comment about "list order encodes specificity") to describe the new two-tier (exact-match then substring) structure, so the code stays self-documenting. Verify by reading the updated comments alongside the code they describe.
- [x] 2.7 Per design.md Decision 5, lower `_KIND_DEFAULTS["payment_degradation"]`'s confidence from `0.60` to `0.55` (strictly below `GUARDRAIL_CONFIDENCE_FLOOR`'s default), with an inline comment explaining why (generic/unclassified payment-degradation codes should escalate by default rather than silently auto-act). Verify with a manual call (`_heuristic_diagnosis({"kind": "payment_degradation", "failure_code": "some_wholly_unrecognized_code"})`) returning confidence `0.55`.

## 3. Test coverage

- [x] 3.1 In `backend/agents/tests/test_pipeline.py`, update `test_diagnosis_matches_failure_code_pattern`'s parametrized cases that reference retired invented codes (`network_timeout`, `issuer_unavailable_timeout`, `mandate_charge_failed`) to their real-code equivalents (e.g. `payment_timed_out`→timeout-class root cause, a real mandate code→its new root cause), keeping `insufficient_funds`, `card_declined`, `expired` as-is. Verify with `pytest backend/agents/tests/test_pipeline.py -k test_diagnosis_matches_failure_code_pattern`.
- [x] 3.2 Update `test_diagnosis_rule_order_expired_beats_card_declined_on_overlap` and `test_subscription_failure_card_decline_routes_to_registration_link` / `test_subscription_failure_never_produces_retry_order` to use real codes in place of `card_declined_expired` and `card_declined_mandate` (e.g. `card_expired` directly for the first, since it's now an exact-match table entry rather than a substring-overlap case — or keep `card_declined_expired` as a still-valid substring-fallback-tier regression case per design.md Decision 1's mitigation, since the fallback tier must still handle it correctly even though seed data no longer produces it). Verify both tests pass and still assert the same behavioral guarantees (expired beats declined; subscription never produces `retry_order`).
- [x] 3.3 Add new parametrized/unit test cases covering each new `_CODE_DIAGNOSES` entry from 2.1 — at minimum `authentication_failed`, `card_not_enrolled`, `invalid_vpa`, `vpa_resolution_failed`, the three `*_technical_error` codes, `transaction_limit_exceeded`, `incorrect_cvv`, `debit_instrument_blocked`, `debit_instrument_inactive`, `payment_timed_out`, `request_timed_out` — asserting the expected root cause and, where applicable, confidence relative to the guardrail floor. Verify with `pytest backend/agents/tests/test_pipeline.py`.
- [x] 3.4 Add a test asserting the exact-match tier takes precedence over the substring tier for a code present in both conceptually (e.g. `card_expired` exact match vs. what the `expired` substring rule alone would have produced), per the new spec requirement scenario "An exact known-code match is used even when it would also satisfy a general pattern". Verify with `pytest backend/agents/tests/test_pipeline.py`.
- [x] 3.5 Add a test for `risk_check_failed` (or `payment_risk_check_failed`) asserting it resolves to `escalate` in the decision step regardless of its diagnosis confidence, confirming design.md Decision 2's explicit fraud-hold routing. Verify with `pytest backend/agents/tests/test_pipeline.py`.
- [x] 3.6 Audit `backend/recovery/tests/test_tasks.py` for any dependency on retired invented codes or on specific confidence values that this change touches; confirm its existing codes (`insufficient_funds`, `card_declined`, `invoice_overdue`, `""`) are unaffected and require no changes. Verify with `pytest backend/recovery/tests/test_tasks.py` passing unmodified.
- [x] 3.7 Per design.md Decision 5, update `test_diagnosis_unrecognized_code_falls_back_to_kind_default` to assert the new `0.55` confidence (strictly below the floor) instead of `>= 0.60`, and add or extend a `recovery/tests/test_tasks.py` case asserting that a `payment_degradation` transaction with a wholly unrecognized failure code now resolves to `Transaction.Status.ESCALATED` (not an autonomous action), confirming the guardrail actually fires on the new default. Verify with `pytest backend/agents/tests/test_pipeline.py backend/recovery/tests/test_tasks.py`.
- [x] 3.8 Run the full backend test suite (`pytest backend/`) and confirm all tests pass, including the `heuristic_only` fixture-based tests in both `agents/tests/test_pipeline.py` and `recovery/tests/test_tasks.py`.

## 4. Documentation touch-ups

- [x] 4.1 If `README.md` or any other doc references the old invented failure-code vocabulary as an example, update those references to real Razorpay codes for consistency. Verify by searching the repo for the retired code strings (`card_declined_expired`, `network_timeout`, `issuer_unavailable_timeout`, `mandate_charge_failed`, `card_declined_mandate`) outside of test/history context and confirming none remain as live documentation examples.
