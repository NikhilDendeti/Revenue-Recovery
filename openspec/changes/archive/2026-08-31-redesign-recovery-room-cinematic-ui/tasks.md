## 1. Design system foundation

- [x] 1.1 Rewrite `frontend/src/index.css` as the token layer: `@theme` tokens for the near-black/charcoal elevation ramp, brand red, the six status colours, the type scale, radii, shadows/glows, and motion easings + durations; plus base styles (body ground, selection, scrollbar, `focus-visible` ring, `scroll-margin-top` for anchored sections), the keyframes used by the UI, and a global `prefers-reduced-motion` block. Verify: no component needs an arbitrary hex; `npm run build` emits CSS containing the new custom properties.
- [x] 1.2 Update `frontend/index.html` — title/meta, the on-brand inline favicon, `color-scheme: dark`, a theme-colour meta, and the font links reduced to one display/body sans plus the existing mono (serif face removed). Verify: no request is made for the removed serif family and the page has no flash of an unstyled serif heading.
- [x] 1.3 Extend `frontend/src/lib/format.js` with the full status/outcome/flow/actor descriptors — label, glyph name, and token classes for each of `open | processing | recovered | failed | escalated | held`, each `kind`, and each guardrail result — keeping the existing `inr`, `timeAgo`, `OUTCOME_STYLE`, and `STATUS_STYLE` export names working. Verify: every existing import site still resolves and every status renders a label plus a non-colour glyph.
- [x] 1.4 Replace the leftover Vite template assets in `frontend/public/` (`icons.svg` with Bluesky/Discord/GitHub logos, the purple `favicon.svg`) with on-brand equivalents or remove them if unreferenced. Verify: `grep -r` finds no reference to a removed asset and the build has no missing-asset warning.

## 2. Component primitives

- [x] 2.1 Add `frontend/src/components/ui/Icon.jsx` — one inline-SVG glyph map covering the icons the redesign needs, with `aria-hidden` by default and an optional accessible title. Verify: no emoji remains as a load-bearing icon anywhere in `src/components/`.
- [x] 2.2 Add `Button.jsx` and `IconButton.jsx` with the primary / secondary / ghost / danger variants and sizes, each implementing hover, `focus-visible`, active, disabled, and loading states, and never dropping below the minimum touch-target size on touch viewports. Verify: a disabled button is both visually distinct and non-activatable by pointer and keyboard.
- [x] 2.3 Add `Card.jsx`, `Badge.jsx`, `Skeleton.jsx`, `EmptyState.jsx`, and `Tooltip.jsx` on the token layer. Tooltip must be dismissible and must not trap focus. Verify: each renders from tokens only, and `Badge` renders label + glyph together (spec: status is never communicated by colour alone).
- [x] 2.4 Add the toast system as `frontend/src/lib/toastContext.js` (context object + `useToast` hook — no components) plus `frontend/src/components/ui/ToastProvider.jsx` (provider + viewport — components only), split so neither module mixes component and non-component exports. Toasts must be announced to assistive technology and auto-dismiss without stealing focus. Verify: `npm run lint` reports no `react/only-export-components` warning for either module.

## 3. Navigation and shell

- [x] 3.1 Rebuild `Header.jsx`: sticky, transparent over the hero and solidifying on scroll, with the wordmark, section navigation with an active red indicator, the expanding search field, the live/disconnected pill, the primary replay CTA, and an operator menu (keyboard-navigable, Escape-closable, closes on outside click). Verify: the active indicator is not colour-only, and the menu is fully operable by keyboard.
- [x] 3.2 Add `MobileNav.jsx` — a fixed bottom navigation bar shown below the `lg` breakpoint (moved up from `md` during the final pass: at 768-840px the header's own minimum content width overflowed its track once the section nav was present), with full-size touch targets, an active state, and `env(safe-area-inset-bottom)` padding; give `<main>` matching bottom padding. Verify: at a 360px viewport no page content is obscured by the bar and every target meets the minimum touch-target size (spec: small viewports get a touch-designed navigation).
- [x] 3.3 Add section-anchor navigation with one `IntersectionObserver` driving the active section for both navs, `scroll-margin-top` sized to the sticky header, and reduced-motion-aware smooth scrolling. Verify: activating each nav item lands with the section heading visible below the header, not beneath it (spec: the header does not obscure a navigated-to section).
- [x] 3.4 Add the skip link and semantic landmarks (`header` / `nav` / `main` / `footer`) with a coherent heading order across the shell. Verify: the skip link is the first focusable element, is visible on focus, and moves focus to `<main>`.

## 4. Hero, KPIs, and the voice moment

- [x] 4.1 Add `Hero.jsx` — the recovered-value billboard with batch context, the recovery-rate meter, the live indicator, the primary red replay CTA and a subordinate secondary CTA, over a CSS-authored cinematic background with a readability overlay. Verify: hero text meets contrast over the darkest and lightest points of its background (spec: hero text remains readable over its background treatment).
- [x] 4.2 Rebuild `SummaryStrip.jsx` as the KPI rail on the new card primitive, with per-tile icon, label, value, and supporting detail, reading every field from the existing `/summary/` payload. Verify: no new field is expected from the endpoint, and the rail reflows without overflow from 360px to 1920px.
- [x] 4.3 Rebuild `VoiceMoment.jsx` as a high-prominence cinematic surface — transcript, customer response, promise-to-pay chip, dismiss — keeping its existing props and dismissal behaviour. Verify: it renders from the unchanged `voice` payload fields and dismissing it still clears the moment.
- [x] 4.4 Add skeleton states for the hero and KPI rail covering the pre-`summary` render. Verify: with the summary unresolved, neither surface shows a blank area nor a misleading empty state (spec: a pending surface shows a loading state).

## 5. Transaction browsing

- [x] 5.1 Add `TransactionCard.jsx` — fixed-ratio poster card with flow badge, customer, amount, failure code, status pill, in-flight progress affordance, and a desktop hover reveal; activatable by click, tap, and keyboard. Verify: everything the hover reveals is reachable on touch by activating the card (spec: additional card detail is not hover-gated).
- [x] 5.2 Add `ContentRow.jsx` — a horizontally-scrollable, scroll-snapping row with edge fades, pointer chevron controls that appear on row hover and hide at the scroll extents, keyboard scrolling, and no page-level horizontal overflow. Verify: at 360px the row scrolls internally while the page body does not scroll horizontally (spec: a row is scrollable by pointer, keyboard, and touch).
- [x] 5.3 Add `SearchFilterBar.jsx` — free-text search over customer, id, and failure code plus flow and status filter chips, with visible active-filter indication and a single clear-all action, applied client-side to the already-fetched list. Verify: filtering issues no network request (spec: filtering issues no additional server call).
- [x] 5.4 Wire the state-grouped rows into `Dashboard.jsx` in urgency order, omitting rows with no matching transactions, and add the two distinct empty states — "no transactions at all" and "filters matched nothing, clear them". Verify: clearing filters from the filtered-empty state restores the rows (spec: a search matching nothing explains itself).

## 6. Live panels and audit trail

- [x] 6.1 Rebuild `RecoveryTicker.jsx` on the design system with per-outcome colour rails, label + glyph, customer/amount/time metadata from the unchanged ticker payload (`customer_id`, not `customer_name`), an accessible name per row, the retained insert animation, and a fluid clamp height replacing `h-[520px]`. Verify: the `MAX_FEED` buffer behaviour is unchanged and the panel fits a 768px-tall viewport without a second page scroll.
- [x] 6.2 Rebuild `GuardrailConsole.jsx` as a premium console — monospace ledger, blocked-vs-passed treatment carrying a glyph as well as colour, rule-name tooltips, accessible row names, and the same fluid height. Verify: a blocked entry is identifiable without colour (spec: status is never communicated by colour alone).
- [x] 6.3 Rebuild `AuditTrail.jsx` as a premium table at `md` and above and a stacked card list below it, preserving row selection by click and keyboard, the selected-row indication, and the existing empty state. Verify: at 360px the entry's fields read vertically with no horizontal scroll (spec: a narrow viewport gets a stacked layout).
- [x] 6.4 Add skeleton and error states to all three panels, including a live-connection-dropped state that keeps already-received events visible. Verify: dropping the WebSocket shows a disconnected indication, not a request-failure state, and the buffered ticks remain (spec: a dropped live connection is distinguishable from an error).

## 7. Reasoning-chain dialog

- [x] 7.1 Convert `ChainDrawer.jsx` to a true modal dialog: `role="dialog"`, `aria-modal`, an accessible name, focus moved in on open and restored to the invoking control on close, Tab cycling within the panel, Escape to close, and `body` scroll lock while open. Verify: Tab from the last focusable element returns to the first, and Escape closes and restores focus (spec: the dialog manages focus and can be dismissed by keyboard).
- [x] 7.2 Add the tabbed chain view — Timeline (default), Diagnosis, Decision, Actions, Guardrails, Scheduled — rendering the collections the chain endpoint already returns, with the raw audit payloads still inspectable verbatim behind a disclosure. Verify: a transaction with a diagnosis and a decision shows both in readable form rather than only its audit entries (spec: the full reasoning chain is presented).
- [x] 7.3 Give every tab an empty state so a tab with no records stays present and says so. Verify: a transaction with no guardrail events still shows the Guardrails tab, stating it has none (spec: a section with no records explains itself).
- [x] 7.4 Restyle the dialog header and keep the high-value-receivable voice-showcase trigger with its existing threshold and call. Verify: the trigger still appears for the same transactions and still issues the same request.
- [x] 7.5 Add the dialog's loading skeleton and a fetch-failure state with retry, replacing the current unguarded `.finally`-only handling. Verify: a failing chain fetch shows an error with a working retry rather than an indefinite loading line (spec: a failed request is surfaced to the operator).

## 8. Login screen

- [x] 8.1 Rebuild `Login.jsx` as the cinematic entry screen — full-bleed CSS backdrop with a readability overlay, brand mark, elevated form card, labelled inputs, a password reveal toggle, primary red submit with a loading state, and an error presented in text and associated with the form. Verify: login, failure, and loading behave exactly as before and each input has an associated label (spec: form inputs are labelled and errors are explained).

## 9. Error surfacing and state plumbing

- [x] 9.1 Replace the silent `.catch(() => {})` paths in `Dashboard.jsx` (transactions refresh, voice showcase) and `useRecoveryRoom.js` (summary refresh) with toast plus inline retry where the surface owns data, adding `error` to `useRecoveryRoom`'s return value additively. Verify: with the backend stopped, the dashboard shows a visible failure and a retry rather than a blank panel (spec: a failed request is surfaced to the operator).
- [x] 9.2 Give the batch-replay and voice-showcase controls acceptance confirmation and a pending state that blocks re-triggering. Verify: the replay button cannot be double-fired while pending and confirms acceptance (spec: an operator action confirms that it was accepted).
- [x] 9.3 Add a top-level error boundary so a render error shows a designed recovery screen rather than a blank page. Verify: a forced render error renders the boundary's screen with a working reload action.

## 10. Final quality pass

- [x] 10.1 Run `npm run lint` and `npm run build` in `frontend/` and fix everything reported. Verify: both complete with no error and no new warning.
- [x] 10.2 Audit every surface at 360 / 768 / 1024 / 1440 / 1920 px for horizontal page overflow, clipped or misaligned content, and cramped touch targets; fix what is found. Verify: `document.documentElement.scrollWidth <= clientWidth` at every checked width (spec: no supported viewport scrolls the page horizontally).
- [x] 10.3 Audit contrast for every text/background and border pairing in the token layer and fix any pairing below the AA threshold for its size. Verify: body and metadata text clear 4.5:1 and large text clears 3:1 against the surface each is used on.
- [x] 10.4 Audit keyboard operability end to end — skip link, header, mobile nav, search, filters, rows, cards, table, dialog, tabs, toasts — and fix any focus trap, unreachable control, or missing accessible name. Verify: the full flow from login to opening and closing a reasoning chain is completable with the keyboard alone.
- [x] 10.5 Verify no functional regression against the pre-redesign behaviour: login/refresh/session-expiry, the WebSocket feed and its `MAX_FEED` buffer, batch replay, voice showcase, chain fetch-on-select, and the `data.results ?? data` list handling. Verify: no new server request or request parameter is issued and `package.json` dependencies are unchanged (spec: existing dashboard behaviour is preserved).
- [x] 10.6 Update `README.md`'s dashboard description and `CLAUDE.md`'s frontend repo-layout section to match the new component set. Verify: neither file names a component that no longer exists nor omits one that now does.
