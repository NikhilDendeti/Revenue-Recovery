Green gate, run at the end of every task: `cd backend && .venv/Scripts/python.exe -m pytest -q`.
The baseline is **`79 passed, 1 skipped`**. New tests may push the passed count up; the
skipped count must stay at exactly 1 (`test_contact_cap_race_only_one_of_two_concurrent_contacts_clears`,
which SQLite cannot run), and no pre-existing test may fail, be deleted, or be weakened to
keep the count. A rising pass count is progress; a rising skip count is a regression in
disguise.

## 1. Vocabulary — DTOs, exceptions, interfaces, fakes, enforcement

- [x] 1.1 Add `recovery/dtos.py` with frozen dataclasses for every value that crosses a layer boundary: `TransactionDTO` (all 13 `TransactionSerializer.Meta.fields`, `amount` as `Decimal`, including `merchant_id`), `DiagnosisDTO`, `DecisionDTO`, `ActionDTO`, `GuardrailCheckDTO`, `GuardrailVerdictDTO`, `ContactSlotDTO`, `ScheduledActionDTO`, `AuditEntryDTO`, `GatewayArtifactDTO`, `PipelineResultDTO`, `WebhookEventDTO`. Verify: `python -c "import recovery.dtos"` succeeds and the module imports nothing from `django.db`.
- [x] 1.2 Add `recovery/exceptions.py` with the domain errors interactors raise: `TransactionNotFound`, `TransactionNotOpen`, `UnrecognizedWebhookEvent`, `GatewayError`, `GatewayArtifactNotFound`. Verify: `GatewayArtifactNotFound` is a subclass of `GatewayError` so a single `except GatewayError` still catches both.
- [x] 1.3 Add `recovery/interfaces/` with `storage_interface.py`, `presenter_interface.py` and `ports.py` (`PaymentGatewayInterface`, `DiagnosisPipelineInterface`, `TaskQueueInterface`, `ClockInterface`, `RandomnessInterface`) as ABCs with no implementation. Verify: every method body is `raise NotImplementedError` or `...`, and the package imports no Django.
- [x] 1.4 Add `recovery/interactors/__init__.py` containing THE RULE verbatim as its module docstring, and nothing else. Verify: the docstring states the four qualifying conditions and names the read-path exemption.
- [x] 1.5 Add `recovery/tests/fakes.py` with `FakeStorage`, `FakeGateway`, `FakePipeline`, `FakeTaskQueue`, `FakeClock`, `FakeRandomness`, `FakePresenter` — in-memory doubles recording their calls. Verify: each implements its ABC (`isinstance` check passes) and needs no database.
- [x] 1.6 Add `recovery/tests/test_layer_boundaries.py` enforcing: (a) no module under `recovery/interactors/` or `recovery/guardrails/rules.py` imports any of `django`, `rest_framework`, `celery`, `channels`, `requests`, `random`, `agents`, `langgraph` (AST scan, not a string grep); (b) exactly one module outside `models.py`/`migrations/`/`admin.py`/`serializers.py`/`tests/` imports `AuditLogEntry`; (c) no module calls `.update()` on an `AuditLogEntry` queryset. Verify: the test passes now (`interactors/` is empty) and fails if a banned import is added to a scratch file.
- [x] 1.7 Add `recovery/tests/test_interactors/__init__.py` so the new suite is collected — `pytest.ini` sets `testpaths = recovery/tests agents/tests`, so anything outside `recovery/tests/` is silently not run. Verify: `pytest --collect-only -q | wc -l` grows by exactly the number of new tests.

## 2. Ports, adapters, and the outcome rule

- [ ] 2.1 Add `recovery/domain_rules.py` with the pure `resolve_outcome(confidence, draw) -> bool` returning `draw < min(0.95, max(0.05, confidence))`. Verify: `resolve_outcome(0.0, 0.049) is True`, `resolve_outcome(0.0, 0.051) is False`, `resolve_outcome(1.0, 0.949) is True`, `resolve_outcome(1.0, 0.951) is False` — the clamp is exercised at both ends.
- [ ] 2.2 Add `recovery/adapters/runtime.py` with `DjangoClock` (reaching time via `timezone.now()`, exposing `now()`, `local_hour(at)`, `local_window_start(at, hour)`) and `SystemRandomness.uniform(key)`. When `settings.RECOVERY_OUTCOME_SEED` is set, `uniform` returns `random.Random(f"{seed}:{key}").random()`; unset, it returns `random.random()`. Verify: a test asserting 200 distinct keys under one seed produce **more than 50 distinct values** — a per-process `random.Random(seed)` would return one constant and pass a naive "same twice" check.
- [ ] 2.3 Add `recovery/adapters/razorpay_gateway.py` translating `razorpay_client` calls into the port, resolving `razorpay_client.<fn>` **at call time** (so `mock.patch` on the module keeps working) and converting `RazorpayError` with `status_code == 404` into `GatewayArtifactNotFound`, every other `RazorpayError` into `GatewayError`, and letting all non-`RazorpayError` exceptions propagate untouched. Verify: a unit test that patching `recovery.razorpay_client.reopen_order_checkout` with a 404 `RazorpayError` surfaces as `GatewayArtifactNotFound`, and with a `RuntimeError` surfaces as `RuntimeError`.
- [ ] 2.4 Add `recovery/adapters/langgraph_pipeline.py` wrapping `agents.pipeline.run_pipeline` behind `DiagnosisPipelineInterface`, and `recovery/adapters/task_queues.py` with `CeleryTaskQueue` (each `enqueue` wrapped in `transaction.on_commit` when inside an atomic block) and `InlineTaskQueue` (carrying `stagger_seconds` and an `on_progress` hook so `replay_batch --sync` keeps its pacing and its every-10 progress lines). Verify: `InlineTaskQueue` sleeps between dispatches and does not re-enter itself.
- [ ] 2.5 Add `recovery/wiring.py` as the composition root with one `build_*` factory per interactor. Verify: `wiring` is imported by `tasks.py`, `guardrails/__init__.py` and `views.py` only — never by `consumers.py`, `routing.py` or `ws.py`.
- [ ] 2.6 Add `RECOVERY_OUTCOME_SEED` to `config/settings.py` (default `None`) and to `backend/.env.example` with a comment stating it makes a replay reproducible and is unset by default. Verify: unset, `SystemRandomness.uniform` calls `random.random()`.
- [ ] 2.7 Change `recovery/tasks.py` line 169 only, to `recovered = resolve_outcome(diagnosis_confidence, randomness.uniform(str(txn.id)))`. Verify: full suite green; `test_process_transaction_event_cleared_records_recovered_or_failed` still passes because the expression is mathematically identical.
- [ ] 2.8 Extend `test_layer_boundaries.py` with a check that `import config.asgi` leaves no `langgraph` or `agents.pipeline` key in `sys.modules`. Verify: it passes today, and fails if `wiring` is imported at `consumers.py` module scope.

## 3. Guardrails — six pure rules behind a signature-preserving facade

- [ ] 3.1 Convert `recovery/guardrails.py` into a package: `guardrails/__init__.py` (facade, **keeping `from django.utils import timezone` bound at module level** so the three `patch("recovery.guardrails.timezone.now")` tests keep resolving), `guardrails/context.py` (`GuardrailContext`, a plain dataclass carrying every value the rules need pre-fetched), `guardrails/rules.py` (the six pure rule functions), `guardrails/evaluator.py` (ordering and short-circuit logic). Verify: `test_guardrails.py` passes unmodified, all 15 tests.
- [ ] 3.2 Implement the six rules as pure functions over `GuardrailContext`, each returning a `GuardrailCheckDTO` (or `None` where the rule does not apply). Preserve exactly: rule 2 emits **no** check unless the action is a retry action; rules 1–3 set escalate and short-circuit before rules 4–6 run; rule 6 owns the `if next_window <= now: next_window += timedelta(days=1)` bump. Verify: a low-confidence `NEW_PAYMENT_LINK` context returns exactly **two** checks; a low-confidence `RETRY_ORDER` context returns **three**.
- [ ] 3.3 Add `recovery/storages/recovery_storage.py` with the three guardrail-path methods: `count_prior_retries`, `reserve_contact_slot` (owning `atomic()` + `select_for_update()` + the `GuardrailEvent` write in **one** transaction), and `log_guardrail_event`. Verify: `test_contact_cap_race_...` still skips on SQLite for the same documented reason, and a DB-free test asserts `select_for_update` appears in the compiled query.
- [ ] 3.4 Populate `GuardrailVerdict.events` with the checks the evaluation actually produced, and change `tasks.py` (lines 245–256) to read `verdict.events` instead of re-querying `GuardrailEvent` twice with the magic `[:6]`. Verify: `decision.guardrail_checks_passed` still holds `{rule_name, rule_result}` dicts and the Guardrail Console still receives one frame per check.
- [ ] 3.5 Add `recovery/tests/test_interactors/test_guardrail_rules.py` — DB-free unit tests for all six rules including both short-circuit shapes and the compliance-hours next-day rollover. Verify: asserts `verdict.hold_until > now` for a 21:00 IST context, which no existing test covers.

## 4. Storage and the write path — `ExecuteAction` and `Escalate`

- [ ] 4.1 Extend `recovery/storages/recovery_storage.py` with the action-path methods (`get_transaction`, `record_action`, `set_status`, `append_audit`, `get_summary`) plus the `_txn_dto` model-to-DTO converter. Verify: `append_audit` is the only write path to `AuditLogEntry` and the interface exposes no update or delete verb.
- [ ] 4.2 Add `recovery/presenters/recovery_room_presenter.py` owning all four WebSocket frame shapes, taking `publish=ws.push` as an injected callable so `recovery/ws.py` is **not modified**. Verify: `present_ticker` converts the summary to a plain dict; a frame-shape test pins the exact key sets `useRecoveryRoom.js` switches on for `ticker`, `guardrail`, `audit` and `voice`.
- [ ] 4.3 Add `recovery/interactors/escalate_transaction.py` covering all three escalation causes (guardrail, API failure, unexpected error) with the `event_type` distinguishing them. Verify: each cause writes an `ESCALATE` action, sets status `escalated`, appends an audit entry and pushes an escalated ticker frame.
- [ ] 4.4 Add `recovery/interactors/execute_action.py` holding the 404-fallback and outcome logic, branching on `except GatewayArtifactNotFound` rather than an HTTP status check. Verify: the `_recoverai_fallback` note keeps its exact three keys, and the channel flips to `EMAIL` on fallback.
- [ ] 4.5 Gut `tasks.py` lines 72–183, keeping `_execute_action` as a delegate that converts its model argument via `_txn_dto` before calling the interactor. Verify: `from recovery.tasks import _execute_action` at `test_tasks.py:17` still resolves.
- [ ] 4.6 Retarget the four DI-impossible tests in `test_tasks.py` onto `recovery.razorpay_client.<fn>` patches, keeping every assertion verbatim. Verify: `pytest -q` before and after gives identical pass/skip counts, and `test_execute_action_falls_back_to_payment_link_on_404_order` still asserts on `api_response["short_url"]`.
- [ ] 4.7 Add `recovery/tests/test_interactors/test_execute_action.py` exercising the 404 fallback, the fallback-also-fails path and the `_recoverai_fallback` note with no database and no patching. Verify: the file contains no `django_db` marker and no `mock.patch`.

## 5. Pipeline and scheduling interactors — `tasks.py` becomes transport

- [ ] 5.1 Add `recovery/interactors/process_transaction_event.py` orchestrating detect → diagnose → decide → guardrail → act, carrying the three-reason comment on why the body is **not** wrapped in `atomic()`. Verify: `test_process_transaction_event_escalates_on_unexpected_pipeline_error` still finds the `detected` audit entry after the failure.
- [ ] 5.2 Add `recovery/interactors/dispatch_scheduled_action.py`, explicitly **not** calling `claim_open_transaction` — its transaction is `HELD`. Verify: a held transaction dispatches; add a comment naming the deadlock this avoids.
- [ ] 5.3 Add `recovery/interactors/{sweep_scheduled_actions,replay_batch,trigger_voice_showcase}.py`. Verify: `replay_batch` returns the same `{"queued": n}` shape and orders by `created_at` ascending.
- [ ] 5.4 Reduce `tasks.py` to five `@shared_task` shells, each pinned with `name="recovery.tasks.<fn>"` and each at most three lines. Verify: `CELERY_BEAT_SCHEDULE`'s hardcoded `"recovery.tasks.sweep_scheduled_actions"` still resolves; the file is under 80 lines.
- [ ] 5.5 Retarget the one remaining patch (`recovery.tasks.run_pipeline`) onto the pipeline adapter. Verify: `test_process_transaction_event_escalates_on_unexpected_pipeline_error` passes.
- [ ] 5.6 Add `recovery/tests/test_interactors/test_process_transaction_event.py` — DB-free, asserting the full frame sequence and audit-entry sequence for a cleared, a held and an escalated transaction. Verify: three distinct frame orderings pinned.

## 6. Webhook interactor and `--sync` de-duplication

- [ ] 6.1 Move `WEBHOOK_KIND_MAP` from `views.py:29` into `recovery/constants.py`. Verify: grep confirms nothing else imported it from `views`.
- [ ] 6.2 Add `recovery/interactors/ingest_webhook_event.py` and `recovery/presenters/webhook_presenter.py`, building the 201 body as `TransactionSerializer(txn_dto).data` rather than hand-listing fields. Verify: a test asserts the DTO serialises byte-identically to the model instance, including `amount` quantised as `"1200.00"` by `DecimalField`.
- [ ] 6.3 Replace `WebhookView.post`'s body (lines 128–146) with the interactor call. Verify: an unrecognised event still returns 400 with the same `{"error": "unrecognized event '<x>'"}` body, and a recognised one still returns 201 with the full transaction.
- [ ] 6.4 Point `management/commands/replay_batch.py` at `ReplayBatchInteractor` with `InlineTaskQueue`, preserving the `--stagger` sleep (default 0.2s) and the every-10 progress output. Verify: `python manage.py replay_batch --sync --stagger 1.0` still paces at one per second and prints progress.

## 7. Regression and documentation

- [ ] 7.1 Run the full suite and confirm `79 passed, 1 skipped` with the skip still being `test_contact_cap_race_only_one_of_two_concurrent_contacts_clears`. Verify: no test was deleted or weakened to achieve the count.
- [ ] 7.2 Boot Daphne, the Celery worker and Beat, run a batch replay from the dashboard, and confirm the ticker, guardrail console, audit trail and chain drawer all behave identically to before. Verify: WebSocket frames observed in the browser match the pinned key sets.
- [ ] 7.3 Update `CLAUDE.md`'s repo-layout section to describe the new packages and state THE RULE, so the next contributor places code correctly without reading this change. Verify: the layout block names `interactors/`, `storages/`, `presenters/`, `adapters/`, `interfaces/` and says which paths are exempt.
